# Codtech-Tsk7-Markdown-Converter
A lightweight Markdown to HTML conversion engine built for Codtech IT Solutions (Task 7). This Python application parses standard Markdown elements—including headers, bold text, links, and line breaks—and dynamically compiles them into structured, browser-ready HTML documents.
